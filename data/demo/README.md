# Synthetic demo data

This directory contains fictional destinations and synthetic evidence created only for the public portfolio demo.

- It does not contain scraped UGC, real source URLs, user identifiers, or production analytics.
- `demo://synthetic/...` URLs are provenance markers, not external sources.
- Product and retrieval metrics in the project documentation come from a separate private evaluation snapshot and are not recomputed from these six records.
